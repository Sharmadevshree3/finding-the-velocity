# finding-the-velocity
#include <stdio.h>

int main()
{
  float velocity, distance, time;
  printf("enter the value of distance: ");
  scanf("%f", &distance);
  printf("enter the value of time: ");
  scanf("%f", &time);
  printf("velocity is %f", velocity=distance/time);
    return 0;
